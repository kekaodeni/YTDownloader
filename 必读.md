我要你从零开发一个可以实际使用、可以打包发布的 Windows 11 原生风格 yt-dlp 图形化下载软件。

这不是 Demo、概念验证或单文件示例，而是一个完整的桌面应用项目。请直接在当前工作区创建项目、编写代码、安装必要依赖、运行程序、测试功能、发现问题后修复，直到得到可以正常运行的软件。

==================================================
一、开发技术栈
==================================================

主要技术栈：

- Python 3.12+
- PySide6
- yt-dlp
- FFmpeg / ffprobe
- Windows 11
- PyInstaller
- pytest
- SQLite 或 JSON 用于持久化少量设置和历史记录

优先使用 yt-dlp 官方 Python API，而不是通过 shell 拼接 yt-dlp 命令。

FFmpeg 可以通过 subprocess 调用，但：
- 禁止 shell=True
- 正确处理 Windows 路径、中文路径、空格路径
- 正确处理 Unicode 文件名
- 捕获 stdout/stderr
- 防止 GUI 主线程阻塞

所有下载、FFmpeg、网络请求、缩略图生成等耗时任务必须运行在 Worker Thread / QThread / QThreadPool 中。

绝对禁止在 GUI 主线程执行耗时任务。

==================================================
二、必须使用我已经安装的 Skills
==================================================

开发过程中主动使用并遵循以下 Skills：

1. PySide6 Fluent UI
   - PySide6 桌面应用架构
   - Fluent 风格控件
   - Windows 11 UI
   - DPI
   - Dark/Light Theme
   - Widget 设计

2. Fluent Windows 11 UI
   - Windows 11 Fluent Design
   - 简洁现代的视觉设计
   - 圆角
   - 卡片
   - 留白
   - Typography
   - Hover / Pressed / Disabled 状态

3. Terminal GUI Hybrid
   - 日志和 GUI 的合理结合
   - 下载任务状态
   - 错误报告设计

4. Settings First UI
   - 下载目录
   - FFmpeg
   - 软件设置
   - 设置持久化

5. App Feature Craft
   - 每一个功能都必须完整实现，而不是只做 UI

6. App Bug Forensics
   - 出现 Bug 时根据日志定位根因

7. Diagnose
   - reproduce
   - instrument
   - isolate
   - fix
   - regression test

8. TDD
   - 对非 GUI 核心逻辑编写测试

9. Ffmpeg Usage
   - 视频缩略图生成
   - ffprobe 获取时长
   - FFmpeg 调用

10. App Release Readiness
   - 最后检查 Windows 打包和发布质量

不要为了“使用 Skill”机械调用所有 Skill。

根据当前任务选择相关 Skill，但整个项目开发周期必须覆盖上述能力。

==================================================
三、产品目标
==================================================

开发一个：

“简洁、漂亮、几乎零学习成本的 Windows 11 YouTube 视频下载器”。

核心理念：

用户打开程序后：

粘贴 YouTube 链接
→ 自动识别视频
→ 选择清晰度
→ 点击下载

尽可能减少操作步骤。

不要把 yt-dlp 的几十个参数直接暴露给普通用户。

基础界面应该像成熟商业桌面应用，而不是：

- 开发者工具
- Python Demo
- Tkinter 工具
- 网页直接套壳
- 满屏输入框
- 满屏按钮
- 满屏边框
- Bootstrap 风格
- Material Design 风格

==================================================
四、UI设计要求
==================================================

整体：

Windows 11 Fluent Design 风格。

要求：

- 极简
- 精致
- 清爽
- 专业
- 原生桌面软件感
- 大量合理留白
- 清晰的信息层级
- 柔和圆角
- 克制的阴影
- 不滥用卡片
- 不滥用渐变
- 不滥用图标
- 不要五颜六色

支持：

- Windows 11
- 100% / 125% / 150% / 175% / 200% DPI
- 中文系统
- 中文路径
- Dark Mode
- Light Mode
- 跟随系统主题

默认跟随 Windows 系统主题。

窗口最小尺寸要合理。

推荐初始窗口约：

1100 × 720

但必须支持自由缩放。

不要把所有内容全部堆在一个页面。

建议整体布局：

左侧 Navigation：

下载
历史记录
设置

底部：
关于

主界面默认进入“下载”。

==================================================
五、下载主页
==================================================

这是最重要的页面。

顶部放一个非常醒目的 URL 输入区域。

例如：

┌───────────────────────────────────────────────┐
│ 粘贴 YouTube 视频链接……                 解析 │
└───────────────────────────────────────────────┘

同时支持：

Ctrl + V

如果剪贴板中已经存在 YouTube 链接，可以提供轻量提示，但不要未经允许自动开始下载。

输入链接后：

自动识别是否为合法 YouTube URL。

用户点击“解析”或者按 Enter：

开始获取：

- 视频标题
- 视频缩略图
- 视频时长
- 作者 / Channel
- 可用视频格式
- 清晰度
- FPS
- Codec
- 文件大小（如果 yt-dlp 能获得）
- 是否需要视频音频合并

解析期间：

显示 skeleton/loading/progress 状态。

禁止窗口假死。

==================================================
六、视频信息卡片
==================================================

解析完成后显示一个漂亮的视频卡片。

建议：

左侧：
16:9 大缩略图

右侧：

视频标题

Channel
视频时长

清晰度：
[ 1080p ▼ ]

文件名：
[ 原视频标题 ]

保存到：
[ D:\Videos                    📁 ]

底部：

[ 下载 ]

不要默认显示大量技术参数。

技术信息可以放到：

“更多信息”

或者 Tooltip。

==================================================
七、清晰度选择
==================================================

必须能够选择不同画质。

不要直接把 yt-dlp format_id：

137
399
616
248

这种东西展示给普通用户。

应该整理成：

2160p 4K
1440p 2K
1080p
720p
480p
360p

如果有：

60 FPS

应该显示：

1080p 60 FPS

可以在高级信息中显示：

AV1
VP9
H.264

默认优先推荐：

质量较高且兼容性较好的格式。

如果最佳视频流没有音频：

自动选择对应最佳音频流并使用 FFmpeg 合并。

用户不需要自己处理 video+audio。

下载完成的视频必须可以正常播放并包含音频。

格式选择逻辑应该独立成 service/module，并写测试。

==================================================
八、文件名
==================================================

默认：

YouTube 原视频标题

用户可以修改。

必须：

- 自动处理 Windows 非法文件名字符
- 防止：
  \ / : * ? " < > |
- 正确处理 emoji
- 正确处理中文
- 防止文件名为空
- 防止过长路径

用户输入文件名时不要强制要求扩展名。

最终扩展名根据输出格式自动决定。

==================================================
九、下载目录
==================================================

默认保存目录：

Windows 用户 Videos 文件夹。

用户可以：

点击文件夹按钮

选择其他目录。

选择结果必须持久保存。

关闭程序重新打开后仍然记住。

设置页面也可以修改默认下载路径。

==================================================
十、下载进度
==================================================

这是硬性要求。

下载过程中必须同时显示：

1. ProgressBar
2. 百分比
3. 实时下载速度

三者缺一不可。

例如：

██████████████░░░░░░

67%

8.4 MB/s

同时建议显示：

已下载：
845 MB / 1.26 GB

预计剩余：
00:48

当前阶段：

正在下载视频
正在下载音频
正在合并视频与音频
正在处理文件
已完成

yt-dlp progress_hooks 应该用于实时获取：

downloaded_bytes
total_bytes
total_bytes_estimate
speed
eta
status

注意：

speed 可能为空
total_bytes 可能为空

代码必须正确处理这些情况。

下载速度需要自动转换：

B/s
KB/s
MB/s
GB/s

不要阻塞 GUI。

GUI 更新频率要合理，避免 progress hook 高频刷新导致界面卡顿。

==================================================
十一、下载任务卡片
==================================================

下载开始后创建任务卡片。

任务卡片包含：

缩略图
视频标题
清晰度
进度条
百分比
下载速度
文件大小
状态

例如：

┌──────────────────────────────────────────────┐
│ [ Thumbnail ] 视频标题                       │
│                                              │
│ 1080p · MP4                                  │
│                                              │
│ █████████████████░░░░ 73%                    │
│                                              │
│ ↓ 8.7 MB/s            920 MB / 1.28 GB       │
└──────────────────────────────────────────────┘

完成后：

显示：

✓ 下载完成

提供：

打开文件
打开文件夹

不要使用浏览器风格巨大的绿色 Success Alert。

==================================================
十二、视频缩略图
==================================================

必须实现两种缩略图：

第一种：

默认使用 YouTube 视频官方缩略图。

第二种：

下载完成后允许用户从视频中指定某一个时间点生成缩略图。

例如用户点击：

“更换缩略图”

弹出一个简洁 Dialog。

显示：

视频时长：12:46

时间：

[ 00 : 03 : 25 ]

或者：

[ 时间滑块 ]

用户可以选择：

第 205 秒

点击：

预览

使用 FFmpeg：

从本地已经下载的视频第 205 秒提取一帧。

生成 JPEG/PNG。

显示预览。

用户点击：

“设为缩略图”

之后：

该下载任务 / 历史记录中的视频使用这个缩略图。

注意：

不要修改视频文件本身。

这是软件自己的本地预览缩略图。

缩略图文件存放到：

应用数据目录/cache/thumbnails/

使用稳定的视频 ID / hash 命名。

时间必须进行合法性验证：

0 <= timestamp <= duration

使用 ffprobe 获取视频时长。

缩略图生成必须后台运行。

==================================================
十三、历史记录
==================================================

增加一个简洁的历史记录页面。

记录：

- 视频标题
- YouTube URL
- 本地文件路径
- 清晰度
- 下载时间
- 文件大小
- 缩略图
- 下载状态

支持：

打开文件
打开文件夹
复制原视频链接
更换缩略图

不要默认扫描用户整个硬盘。

只保存本软件完成的下载记录。

历史记录可以使用 SQLite。

==================================================
十四、错误处理
==================================================

错误处理必须作为一等功能设计。

任何错误都不能：

程序直接崩溃

或者只显示：

Error
Unknown Error
Download failed

错误窗口应该显示：

下载失败

简短说明：

“无法获取该视频的信息。”

然后有：

错误详情 ▼

展开以后显示技术错误。

同时必须存在：

[ 复制错误报告 ]

按钮。

一键复制完整错误报告到剪贴板。

==================================================
十五、错误报告内容
==================================================

错误报告至少包含：

Application Version
Python Version
Windows Version
yt-dlp Version
FFmpeg Version

Timestamp

Video URL

Selected Format

Output Directory

Current Stage

Exception Type

Exception Message

Traceback

yt-dlp stderr / relevant log

例如：

--------------------------------
YT Downloader Error Report
--------------------------------

Application Version:
0.1.0

Time:
2026-09-01 19:20:31

OS:
Windows 11 24H2

Python:
3.12.x

yt-dlp:
xxxx.xx.xx

FFmpeg:
x.x

URL:
https://youtube.com/...

Stage:
Downloading

Format:
1080p

Exception:
...

Traceback:
...

--------------------------------

但是：

错误报告必须进行敏感信息清理。

禁止复制：

cookies
Authorization
API Key
Token
密码
完整浏览器 Cookie
个人认证信息

如果日志中可能包含这些内容，要在复制前 redact。

==================================================
十六、日志系统
==================================================

使用 Python logging。

日志至少区分：

DEBUG
INFO
WARNING
ERROR

写入：

%LOCALAPPDATA%/<AppName>/logs/

日志使用 rotating file handler。

不能无限增长。

UI 默认不要显示滚动终端。

普通用户只看到必要状态。

技术日志隐藏在：

设置 → 诊断

或者错误详情。

==================================================
十七、设置页面
==================================================

设置页保持精简。

至少包含：

下载：

默认下载目录
默认清晰度

外观：

跟随系统
浅色
深色

工具：

yt-dlp Version
FFmpeg Version

诊断：

打开日志目录
复制系统信息

不要塞几十项普通用户不需要的 yt-dlp 参数。

==================================================
十八、项目架构
==================================================

严禁把所有代码写入 main.py。

建立清晰模块结构。

推荐：

src/
    app.py

    ui/
        main_window.py

        pages/
            download_page.py
            history_page.py
            settings_page.py

        widgets/
            url_input.py
            video_info_card.py
            download_task_card.py
            progress_widget.py
            error_dialog.py
            thumbnail_dialog.py

    core/
        models/
            video_info.py
            download_task.py
            download_progress.py

        services/
            youtube_service.py
            download_service.py
            format_service.py
            ffmpeg_service.py
            thumbnail_service.py
            history_service.py
            settings_service.py

        workers/
            metadata_worker.py
            download_worker.py
            ffmpeg_worker.py

    infrastructure/
        logging_config.py
        paths.py
        database.py

tests/

assets/

scripts/

requirements.txt
README.md

实际结构可以根据需要优化。

要求：

UI
业务逻辑
yt-dlp
FFmpeg
数据库
配置

相互解耦。

==================================================
十九、状态管理
==================================================

为下载任务定义明确状态：

PENDING
FETCHING_METADATA
READY
DOWNLOADING_VIDEO
DOWNLOADING_AUDIO
MERGING
POST_PROCESSING
COMPLETED
FAILED
CANCELLED

不要通过 UI 文本判断状态。

使用 Enum / Model。

==================================================
二十、安全性和稳定性
==================================================

必须考虑：

URL 校验

非法路径

中文路径

Unicode

视频标题非法字符

磁盘空间不足

无 FFmpeg

FFmpeg 无法启动

yt-dlp 获取信息失败

视频不可用

Private Video

Age Restricted

Region Restricted

Network Timeout

403

429

格式不存在

下载中断

用户关闭程序

下载路径失效

用户选择不存在的时间点作为缩略图

所有异常都要被捕获并转化为用户可理解的错误。

不要静默吞异常。

==================================================
二十一、UI细节
==================================================

视觉层级必须明确。

Title：
28px 左右

Section：
18-20px

Body：
14px 左右

不要大量使用：

粗边框
纯黑线条
大面积阴影
渐变背景

Button：

Primary：
下载

Secondary：
解析
浏览
打开文件夹

Danger：
删除 / 取消

Icon 使用统一风格。

不要使用 Emoji 当主要 UI 图标。

Hover / Pressed / Disabled / Focus 状态必须存在。

ProgressBar 要流畅。

加载 metadata 时使用合适 loading 状态。

所有 Dialog 遵循统一 Fluent Design。

==================================================
二十二、测试
==================================================

对以下逻辑写 pytest：

URL validation

filename sanitization

format normalization

quality sorting

human readable file size

human readable speed

progress calculation

thumbnail timestamp validation

settings serialization

error report redaction

不要测试网络本身。

yt-dlp 网络调用使用 mock。

==================================================
二十三、真实运行验证
==================================================

不要只写代码。

开发过程中必须：

1.
创建项目

2.
安装依赖

3.
运行 pytest

4.
启动 GUI

5.
检查有没有 import error

6.
检查窗口是否能正确显示

7.
检查布局

8.
修复错误

9.
再次运行

如果当前环境允许 GUI 截图或 UI 测试：

请实际检查窗口。

不要只根据代码猜测 UI 正常。

==================================================
二十四、打包
==================================================

最终需要能够在 Windows 11 打包。

使用 PyInstaller。

优先：

onedir

而不是一开始追求 onefile。

因为：

yt-dlp
FFmpeg
Qt

依赖较多。

提供：

build.ps1

或者：

scripts/build.ps1

最终输出类似：

dist/
    YTDownloader/
        YTDownloader.exe
        ...

确保启动时：

不显示 cmd 黑窗口。

使用：

--windowed

正确收集 Qt plugins。

处理应用 icon。

如果 FFmpeg 随程序一起发布：

明确处理 ffmpeg.exe
ffprobe.exe

路径。

==================================================
二十五、README
==================================================

最终写 README.md。

包含：

项目介绍

功能

截图位置预留

开发环境

安装依赖

运行：

python -m ...

测试：

pytest

打包：

./scripts/build.ps1

项目目录

已知限制

==================================================
二十六、执行方式
==================================================

不要现在一次性生成全部代码然后停止。

按照工程方式开发。

第一阶段：

检查当前目录和环境。

第二阶段：

根据需求设计架构。

请先输出一个简洁的：

IMPLEMENTATION PLAN

包括：

1. 技术架构
2. UI结构
3. 数据模型
4. yt-dlp 集成方案
5. FFmpeg 集成方案
6. 下载线程方案
7. 错误处理方案
8. 测试方案
9. 打包方案

然后立即开始实现。

不需要等待我再次确认。

==================================================
二十七、开发优先级
==================================================

按以下顺序：

P0：

程序启动
Windows 11 Fluent UI
URL 输入
YouTube metadata 获取
视频缩略图
清晰度选择
文件名
下载目录
下载
ProgressBar
百分比
实时下载速度
错误报告
复制错误报告

P1：

下载历史
视频自选时间点缩略图
Light/Dark/System Theme
打开文件
打开文件夹

P2：

UI polish
缓存管理
日志查看
打包
README
自动化测试完善

不要先做不重要的高级功能。

==================================================
二十八、验收标准
==================================================

项目只有同时满足下面这些条件才算完成：

[ ] Windows 11 下程序正常启动

[ ] UI 精美、现代、简约

[ ] 输入一个 YouTube 视频 URL 可以获取视频信息

[ ] 显示视频官方缩略图

[ ] 可以选择 2160p / 1440p / 1080p / 720p 等实际存在的画质

[ ] 不把 yt-dlp format_id 暴露给普通用户

[ ] 默认文件名是原视频标题

[ ] 用户可以修改文件名

[ ] 可以自定义下载目录

[ ] 下载过程中实时显示 ProgressBar

[ ] 下载过程中实时显示百分比

[ ] 下载过程中实时显示下载速度

注意：

ProgressBar + 百分比 + 下载速度

三项必须同时存在。

[ ] 视频流和音频流需要合并时自动调用 FFmpeg

[ ] 下载完成的视频有声音并可正常播放

[ ] 下载完成后可以打开文件

[ ] 下载完成后可以打开所在文件夹

[ ] 下载任务和历史记录显示缩略图

[ ] 可以指定视频某一秒，通过 FFmpeg 提取帧作为本地缩略图

[ ] 所有耗时操作不会让 GUI 假死

[ ] 错误不会导致整个程序崩溃

[ ] 错误 Dialog 有“一键复制错误报告”

[ ] 错误报告自动隐藏 Cookie / Token 等敏感数据

[ ] 中文路径正常

[ ] 中文标题正常

[ ] Emoji 标题正常

[ ] Light Theme 正常

[ ] Dark Theme 正常

[ ] 高 DPI 正常

[ ] pytest 通过

[ ] PyInstaller 可以成功构建

==================================================

现在开始。

首先检查当前工作区、Python 环境以及已经存在的项目文件。

然后读取相关 Skills。

输出 IMPLEMENTATION PLAN。

紧接着开始实际创建和开发项目，不需要等我确认。